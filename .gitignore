<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تحدي سرعة التفاعل</title>
    <style>
        body { background: #111; color: #fff; font-family: sans-serif; text-align: center; margin: 0; display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; }
        #target { width: 100px; height: 100px; background: #ff4d4d; border-radius: 50%; display: none; cursor: pointer; position: absolute; }
        .score { font-size: 1.5rem; margin-top: 20px; color: #00ff00; }
        .btn { background: #00ff00; color: #000; border: none; padding: 15px 30px; font-weight: bold; border-radius: 5px; cursor: pointer; }
    </style>
</head>
<body>

    <h1>لعبة سرعة التفاعل</h1>
    <p>انتظر ظهور الدائرة الحمراء واضغط عليها بأسرع ما يمكن!</p>
    <button class="btn" onclick="startGame()">ابدأ اللعب</button>
    <div class="score" id="result"></div>
    <div id="target" onclick="hitTarget()"></div>

    <script>
        let startTime;
        const target = document.getElementById('target');
        const result = document.getElementById('result');

        function startGame() {
            result.innerText = "انتظر...";
            target.style.display = "none";
            // وقت عشوائي للظهور بين ثانية و 5 ثواني
            let delay = Math.random() * 4000 + 1000;
            setTimeout(showTarget, delay);
        }

        function showTarget() {
            // تحديد مكان عشوائي في الشاشة
            let x = Math.random() * (window.innerWidth - 100);
            let y = Math.random() * (window.innerHeight - 100);
            
            target.style.left = x + "px";
            target.style.top = y + "px";
            target.style.display = "block";
            startTime = Date.now();
        }

        function hitTarget() {
            let endTime = Date.now();
            let reactionTime = (endTime - startTime) / 1000;
            target.style.display = "none";
            result.innerText = "سرعة تفاعلك: " + reactionTime + " ثانية!";
        }
    </script>
</body>
</html>
